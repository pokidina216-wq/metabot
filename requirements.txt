# ── Vexis — Зависимости ──

# Telegram Bot Framework
# ВАЖНО: aiogram 3.15 несовместим с pydantic 2.10 (требует <2.10).
# Поднято до 3.21, где pydantic 2.10 поддерживается. Не понижать без проверки.
aiogram==3.21.0
aiohttp>=3.9.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# Redis (используется redis.asyncio; отдельный пакет aioredis устарел и не нужен)
redis[hiredis]==5.2.1

# Scheduler
APScheduler==3.10.4

# Config
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Metadata extraction
Pillow==11.1.0
mutagen==1.47.0
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2

# OSINT / HTTP
httpx==0.28.1
python-whois==0.9.4
dnspython==2.7.0

# Utilities
orjson==3.10.13

# Logging
structlog>=24.0.0

# ── Тесты / dev (не требуются в продакшене, но используются в CI) ──
# pytest==9.1.0
# pytest-asyncio==1.4.0
# pgserver==0.1.4
